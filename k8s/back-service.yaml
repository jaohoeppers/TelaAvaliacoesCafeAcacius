apiVersion: v1
kind: Service
metadata:
  name: back-service
  namespace: back-sist-cafe
spec:
  type: NodePort
  selector:
    app: back-app
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30081
